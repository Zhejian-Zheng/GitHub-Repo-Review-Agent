# Healthy Python Service

## Usage

Install the project and run the test suite:

```bash
python -m pip install -e .
python -m unittest discover -s tests
```

## Demo

The service exposes a small health endpoint and includes an example report output.
